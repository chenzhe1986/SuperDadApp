# SuperDadApp · 打包总览

本仓库当前有 3 个可独立打包的安卓应用，各自在自己的目录里用 HBuilderX 完成打包，
详细步骤见各自指南：

| 安装包 | 工程目录 | 详细指南 | 备注 |
|---|---|---|---|
| 全能小学霸（APK / 微信小程序） | `SmartKid/` | `SmartKid/docs/uni-app打包指南.md` | Vue3，题库内置本地 JSON |
| 化学视界（APK） | `LabCraft/uniapp/` | `LabCraft/README-UniApp.md` | web-view 壳，横屏；网页副本不入库，克隆后/改网页后都要先跑 `python LabCraft/sync_uniapp.py` |
| 快乐数计算器（APK） | `HappyNumber/` | `HappyNumber/uni-app打包指南.md` | manifest 未写 vueVersion（默认按 Vue2 编译），代码为 Vue2/Vue3 兼容写法 |
| **爸爸做的超级APP（APK，全家桶）** | `AllInOne/` | `AllInOne/uni-app打包指南.md` | 合并工程，首页导航到 3 个应用；生成物不入库，**打包前必须先在仓库根目录跑 `python sync_all.py`** |

## 通用流程（三个项目一致）

1. 安装 [HBuilderX](https://www.dcloud.io/hbuilderx.html)（App 开发版），登录 DCloud 账号。
2. 菜单：**文件 → 导入 → 从本地目录导入**，选择对应工程目录（LabCraft 导入 `LabCraft/uniapp`）。
3. 调试：**运行 → 运行到手机或模拟器**（安卓手机需开启 USB 调试）。
4. 发布：**发行 → 原生 App-云打包**；测试阶段证书选「使用公共测试证书」，渠道/广告模块不勾选。
5. 发版前在 `manifest.json` 递增 `versionName`（用户可见版本号）与 `versionCode`（必须比上次大）。
6. SmartKid 小程序：**发行 → 小程序-微信**，用微信开发者工具打开
   `unpackage/dist/build/mp-weixin` 上传（详见其指南）。

## 合并工程（AllInOne · 爸爸做的超级APP）

`AllInOne/` 是把 3 个应用合并到一个工程里的"全家桶"：首页提供 3 张应用卡片导航，
代码由 `python sync_all.py` 从各子项目同步生成（3 个子项目保持不变、仍可单独打包）。
生成物一律不入 git 仓库（保持仓库最简），打包流程与上面通用流程一致，另需注意：

1. **克隆仓库后、每次打包前，都必须在仓库根目录运行 `python sync_all.py`**，
   重建全部生成文件（首次拿到代码时 AllInOne 里只有首页等手工文件，不跑无法编译）；
2. 首次导入时按提示生成新 appid（manifest.json 中 appid 为空），并配置应用图标；
3. 详细步骤与真机验证清单见 `AllInOne/uni-app打包指南.md`。
